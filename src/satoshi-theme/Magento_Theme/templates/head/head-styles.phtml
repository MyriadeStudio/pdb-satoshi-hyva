<?php
use Pdb\ThemeHyva\Helper\CssVars;

$helper = $this->helper(CssVars::class);
// Couleur d'accent utilisée par la barre de progression ci-dessous.
// Les nuanciers couleur ne sont plus générés ici : Satoshi consomme désormais
// les tokens sémantiques émis par Pdb_ThemeHyva (css-vars.phtml).
$primaryColor = $helper->getColorPrimary();
?>
<style>
    @font-face {
        font-family: 'Satoshi';
        src: url("<?= $this->getViewFileUrl('fonts/Satoshi-Medium.woff2') ?>") format('woff2'),
        url("<?= $this->getViewFileUrl('fonts/Satoshi-Medium.woff') ?>") format('woff');
        font-weight: 500;
        font-display: swap;
        font-style: normal;
    }

    @font-face {
        font-family: 'Satoshi';
        src: url("<?= $this->getViewFileUrl('fonts/Satoshi-Bold.woff2') ?>") format('woff2'),
        url("<?= $this->getViewFileUrl('fonts/Satoshi-Bold.woff') ?>") format('woff');
        font-weight: 700;
        font-display: swap;
        font-style: normal;
    }

    :root {
        --font-body-family: 'Satoshi', sans-serif;
        --font-body-style: 'normal';
        --font-body-weight: 500;

        --font-heading-family: 'Satoshi', sans-serif;
        --font-heading-style: 'normal';
        --font-heading-weight: 700;

        --progress-bar-color: <?= $primaryColor ?>;
        --progress-bar-height: 2px;

        --radius-lg: .5rem;

        --fade-in-bg: #FDFDFD;

        /* Couleurs : tokens sémantiques émis par Pdb_ThemeHyva (view/base/templates/css/css-vars.phtml) */

        --color-overlay: #18181833;
        --color-overlay-dark: #18181866;
        --header-shadow: 0 10px 8px rgb(0 0 0 / 0.04);
    }

    *,
    *::before,
    *::after {
        box-sizing: inherit;
    }

    html {
        box-sizing: border-box;
        height: 100%;
        text-rendering: optimizeLegibility;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        font-size: 16px;
    }

    body {
        display: grid;
        grid-template-rows: 1fr auto;
        min-height: 100%;
        margin: 0;
        font-size: 1rem;
    }
</style>
